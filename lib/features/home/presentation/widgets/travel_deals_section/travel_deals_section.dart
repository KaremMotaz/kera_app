import 'package:flutter/material.dart';

class TravelDealsSection extends StatelessWidget {
  const TravelDealsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
